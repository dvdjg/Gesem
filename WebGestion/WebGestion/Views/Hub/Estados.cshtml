
@*@{
    ViewBag.Title = "Estados";
}*@
<div class="Estados">
    <h2>Estados</h2>
    <div class="datatable"></div>

    <div class="container-fluid">
        <div class="row">
            <div class="col-md-12">
                <div class="panelForTree"></div>
            </div>
        </div>
        <div class="panel">
            <div class="fancytree-container fancytree-radio fancytree-selector"></div>
            <div class="panel-footer">
                <button class="btn btn-xs tree-expander">Expande todo</button>
                <button class="btn btn-xs tree-collapser">Contrae todo</button>
            </div>
        </div>
    </div>
</div>
<script type="text/javascript">
    console.log("InTo Estados")
    $(function () { //shorthand document.ready function
        console.log("InTo More Estados")
        var vm = getViewModel("Estados"); //new recordListViewModel("Estados"); // Registra en g_viewModels
        vm.initFancyTreeAndUI("div.Estados div.panelForTree", "panelTreeEstados");
        vm.initFancyTree($("div.Estados div.fancytree-selector"), "select");
        // Si no se tiene datatable, llamada para coger records: vm.requestRecordData(0, 200, null, false);
        vm.initDatatable($('div.Estados div.datatable'), { filteredRecords: [], queryToken: "" });
        //setTimeout(function () {
        //    var tree = $("#fancySelector").fancytree("getTree");
        //    tree.val([5, 6]); // El segundo número se impondrá
        //    var vals = tree.val();

        //    //console.log(vals);
        //}, 1000);
    });

</script>




